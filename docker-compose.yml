version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mobile_store_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: mobile_store_user
      POSTGRES_PASSWORD: mobile_store_password
      POSTGRES_DB: mobile_store_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mobile_store_user -d mobile_store_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Optional: pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: mobile_store_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@mobilestore.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
    driver: local
